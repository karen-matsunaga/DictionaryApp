import 'package:flutter/material.dart';

// CRIAÇÃO DA CONTA
Color backgroundAccount = Color.fromRGBO(255, 255, 255, 0.753);

// LOGO DO APLICATIVO
Color appLogo = Color.fromRGBO(45, 89, 249, 0.502);
Color lightBackgroundApp = Color.fromRGBO(65, 179, 204, 0.686);
Color blackBackgroundApp = Color.fromRGBO(11, 23, 26, 0.678);

// CONTEÚDO DOS CONTAINERS
const comando = TextStyle(fontSize: 16, fontWeight: FontWeight.bold);
Color columns = Color(0xFF90CAF9);

// Escrita dos títulos e textos
const titulo = TextStyle(fontWeight: FontWeight.bold, fontSize: 16);
const texto = TextStyle(fontWeight: FontWeight.normal, fontSize: 14);

// MENU DE CONFIGURAÇÕES - TAMANHO DAS FONTES
const Text smallText =
    Text('P', style: TextStyle(fontSize: 16, fontWeight: FontWeight.bold));
const mediumText =
    Text('M', style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold));
const largeText =
    Text('G', style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold));

// TEMA DA APPBAR
// Fundo, barra de pesquisa e ícones
Color backgroundAppBar = Color(0xFF41B3CC);
// Barra de pesquisa
Color backgroundAppBarSearch = Color.fromRGBO(130, 130, 130, 0.49);
Color backgroundAppBarIcons = Colors.black;
Color backgroundIconsWhite = Colors.white;
Color backgroundAppBarLight = Color(0xFF00ACC1);

// TEMA DO DRAWER
// Titulo do Drawer
const menupage = TextStyle(fontSize: 20, fontWeight: FontWeight.bold);

// Fundo do Drawer
Color lightDrawer = Color.fromRGBO(38, 198, 218, 1);
// Color blackDrawer = Color.fromARGB(245, 20, 99, 218);
Color blackDrawer = Color.fromRGBO(2, 39, 174, 1);

// FUNDO DO APLICATIVO EM GERAL
Color lightScaffoldApp = Color.fromRGBO(157, 240, 239, 1);
Color blackScaffoldApp = Color.fromARGB(137, 51, 49, 49);

// TEMA DOS BOTÕES
// Botão principal
Color buttonLight = Color.fromRGBO(45, 89, 249, 0.505);
Color buttonDark = Color.fromRGBO(250, 196, 45, 0.98);

// TEMA DO PERFIL
Color campoPerfil = Color.fromRGBO(65, 179, 204, 0.75);

// Botão secundário
Color buttonLightOne = Color.fromRGBO(255, 255, 255, 0.50);
Color buttonDarkOne = Color.fromRGBO(255, 255, 255, 0.50);

// Tema dos CAMPOS de preenchimento
Color campoclaro = Color.fromRGBO(255, 255, 255, 75);
Color campoescuro = Color.fromRGBO(237, 237, 237, 93);

// CORES DOS TEXTOS
Color lightText = Colors.black;
Color darkText = Colors.white;

// CORES DOS ICONES
Color lightIcon = Colors.black;
Color darkIcon = Colors.white;
Color favoriteIcon = Colors.red;

// CORES DOS CARTOES
Color blackCardColor = Color.fromRGBO(45, 89, 249, 0.505);
Color whiteCardColor = Color.fromRGBO(144, 202, 249, 1);

// SINAL DE ALERTA
Color blackErrorColor = Color.fromRGBO(244, 67, 54, 1);
const Color lightErrorColor = Color.fromRGBO(213, 0, 0, 1);

// TOGGLE BUTTONS
Color lightBorder = Color(0xFF000000);
Color lightBorderSelected = Color.fromRGBO(14, 17, 127, 0.729);
Color lightColorSelected = Color(0xFF00ACC1);
Color lightFillColor = Colors.white70;

// TEMA ESCURO
Color darkBorder = Colors.white;
Color darkBorderSelected = Color(0xFF00ACC1);
Color darkColorSelected = Color(0xFF00ACC1);
Color darkFillColor = Colors.white70;

// BARRA DE PESQUISA
Color darkSearch = Color.fromRGBO(255, 255, 255, 0.58);

// FAVORITE ICON - SEARCH WORD
Color lightIconBorder = Color.fromRGBO(255, 255, 255, 0.512);
Color blackIconBorder = Color.fromRGBO(240, 235, 235, 0.861);

// TEXTOS ESPAÇOS DOS COMPONENTES
// ESPAÇO DA COLUNA DO COMPONENTE
SizedBox espacoComponentes = SizedBox(height: 20);

// ESPAÇO DA LINHA DO COMPONENTE
SizedBox espacoladoComponentes = SizedBox(width: 10);
