import 'package:flutter/material.dart';

// CRIAÇÃO DA CONTA
const Color backgroundAccount = Color.fromRGBO(255, 255, 255, 0.753);

// LOGO DO APLICATIVO
const Color appLogo = Color.fromRGBO(45, 89, 249, 0.505);
const Color backgroundApp = Color.fromRGBO(65, 179, 204, 0.70195);

// CONTEÚDO DOS CONTAINERS
const comando = TextStyle(fontSize: 16, fontWeight: FontWeight.bold);
const Color columns = Color(0xFF90CAF9);

// Escrita dos títulos e textos
const titulo = TextStyle(fontWeight: FontWeight.bold, fontSize: 16);
const texto = TextStyle(fontWeight: FontWeight.normal, fontSize: 14);

// TEMA DA APPBAR
// Fundo, barra de pesquisa e ícones
const Color backgroundAppBar = Color(0xFF41B3CC);
// Barra de pesquisa
const Color backgroundAppBarSearch = Color.fromRGBO(130, 130, 130, 0.49);
const Color backgroundAppBarIcons = Colors.black;
const Color backgroundIconsWhite = Colors.white;
const Color backgroundAppBarLight = Color(0xFF00ACC1);

// TEMA DO DRAWER
// Titulo do Drawer
const menupage = TextStyle(fontSize: 20, fontWeight: FontWeight.bold);

// Fundo do Drawer
const Color drawer = Color(0xFF26C6DA);

// FUNDO DO APLICATIVO EM GERAL
const Color mainThemeApp = Color(0xFF9DF0EF);

// TEMA DOS BOTÕES
// Botão principal
const Color buttonLight = Color.fromRGBO(45, 89, 249, 0.505);
const Color buttonDark = Color.fromRGBO(250, 196, 45, 0.98);

// TEMA DO PERFIL
const Color campoPerfil = Color.fromRGBO(65, 179, 204, 0.75);

// Botão secundário
const Color buttonLightOne = Color.fromRGBO(255, 255, 255, 0.50);
const Color buttonDarkOne = Color.fromRGBO(255, 255, 255, 0.50);

// Tema dos CAMPOS de preenchimento
const Color campoclaro = Color.fromRGBO(255, 255, 255, 75);
const Color campoescuro = Color.fromRGBO(237, 237, 237, 93);

// ESPAÇO DA COLUNA DO COMPONENTE
const SizedBox espacoComponentes = SizedBox(height: 20);

// ESPAÇO DA LINHA DO COMPONENTE
const SizedBox espacoladoComponentes = SizedBox(width: 10);
